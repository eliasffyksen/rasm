{
    "tasks": [
        {
            "type": "shell",
            "label": "build rasm",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}